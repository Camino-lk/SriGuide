<section class="trust">
  <h2 class="cursive-text">Why Trust This Guide?</h2>

  <div class="trust-grid">
    <div class="trust-card">
      <h3 class="cursive-text">Written by Locals</h3>
      <p>
        Written and maintained by <strong>locals</strong> who understand how things
        actually work in Sri Lanka.
      </p>
    </div>

    <div class="trust-card">
      <h3 class="cursive-text">Independent</h3>
      <p>
        Nothing here is sponsored. Just straight facts & useful advice. <strong
          >No ads, no invasive tracking</strong
        >.
      </p>
    </div>

    <div class="trust-card">
      <h3 class="cursive-text">Works Offline</h3>
      <p>
        Install as PWA - <strong>access anytime, even without internet</strong>.
        Perfect for navigating on the go!
      </p>
    </div>
  </div>
</section>

<style>
  .trust {
    margin: 4rem 0;
  }

  .trust h2 {
    margin-bottom: 1.2rem;
  }

  /* grid */

  .trust-grid {
    display: grid;
    gap: 1.4rem;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  }

  /* cards */

  .trust-card {
    display: flex;
    flex-direction: column;
    padding: 1.3rem;
    border-radius: 14px;
    margin-top: 0px;
    border: 1px solid var(--sl-color-gray-5);
    background: var(--sl-color-bg-nav);
    min-height: 140px;
    transition:
      transform 0.18s ease,
      border-color 0.18s ease,
      box-shadow 0.18s ease;
  }

  /* hover */

  .trust-card:hover {
    transform: translateY(-3px);
    border-color: var(--sl-color-accent);
    box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
  }

  /* heading */

  .trust-card h3 {
    margin-bottom: 1rem;
    font-size: 1.5rem;
    color: var(--sl-color-text-accent);
  }

  /* text */

  .trust-card p {
    margin: 0;
    font-size: 0.92rem;
    line-height: 1.45;
    color: var(--sl-color-text-secondary);
  }
</style>
